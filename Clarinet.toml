[project]
name = "stack-mate"
authors = ["Developer"]
license = "MIT"
description = "Chess Game Smart Contract for Stacks - Players vs Computer with Easy and Hard difficulty modes"
requirements = []
cache_dir = "./.cache"
telemetry = false

[contracts.chess-game]
path = "contracts/chess-game.clar"

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
